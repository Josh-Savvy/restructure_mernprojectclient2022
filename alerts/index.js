export const successAlert = (success) => (
  <div className="alert alert-success rounded">{success}</div>
);

export const errorAlert = (error) => (
  <div className="alert alert-danger rounded">{error}</div>
);
